/**
 * @deprecated — Use `import { Sparkline } from '@/components/ui/sparkline'` instead.
 * This re-export exists only for backward compatibility during migration.
 */
export { Sparkline } from '@/components/ui/sparkline';
