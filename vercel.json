{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "//": "Versioned bilateral dataset files are immutable by convention — the URL prefix changes on every annual refresh — so the browser and CDN can cache them forever.",
      "source": "/bilateral/v:version/:file",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    },
    {
      "//": "Manifest can change across deploys, so cache only briefly. Vercel's edge caches it; the SWR window lets new manifests roll out within minutes.",
      "source": "/bilateral/manifest.json",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=300, stale-while-revalidate=86400" }
      ]
    }
  ]
}
